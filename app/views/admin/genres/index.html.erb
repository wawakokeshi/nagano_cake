<div class="container">
  <div class="col-sm-12 mt-3">
   <span class = "h3 bg-secondary text-dark">ジャンル一覧・追加</span>
<p class="form">
<%= form_with model: @genre, url: admin_genres_path, local: true do |f| %>
 <%= f.label :name, "ジャンル名" %>
 <%= f.text_field :name %>
 <%= f.submit "新規登録" %>
<% end %>
</p>

<p class="table">
<table>
 <thead>
  <tr>ジャンル</tr>
  <tr></tr>
 </thead>
 <tbody>
  <% @genres.each do |genre| %>
  <tr>
   <td><%= genre.name %></td>
   <td><%= link_to "編集する", edit_admin_genre_path(genre.id) %></td>
  <tr>
  <% end %>
 </tbody>
</table>
</p>
</div>
</div>